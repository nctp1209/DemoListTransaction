import apisauce from "apisauce";
import SampleData from './SampleData';

const create = () => {
  const getListTransaction = () => SampleData

  return {
    getListTransaction
  };
};

export const apiService = {
  create
};
